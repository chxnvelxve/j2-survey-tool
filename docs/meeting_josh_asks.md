# Meeting — Three asks from Josh

Close the 4–5pm owner meeting with these asks (order matters). Optional fourth
when they are ready for shared storage.

**Close line:** *The machinery is done. The next wow is your branding and your
survey file inside it — one message away.*

---

## 1. One real post-install `.esx`

Representative J2 post-install validation survey.

**Why:** Parser assumptions become confirmed ([`esx_schema.md`](esx_schema.md);
runbook [`phase_08_real_esx.md`](phase_08_real_esx.md)).

**Ideal:** Same engagement as the sample Word deliverable below.

---

## 2. One gold-standard client Word deliverable

Finished document J2 would hand a client (ideally matching that `.esx`).

**Why:** Blueprint for Phase 9 template polish
([`phase_09_template_polish.md`](phase_09_template_polish.md),
[`template_map.md`](template_map.md)).

---

## 3. Confirm threshold / requirements model

Are passing thresholds (e.g. RSSI / SNR) a **built-in per-vertical lookup**, or
a **manual per-job input**?

**Why:** Placeholder profiles already exist in
`app/services/generator/profiles.py` + per-job override JSON — need Josh’s rule
before presenting numbers as authoritative.

---

## Optional 4. Nextcloud access

URL + app password when ready for shared storage.

**Why:** `NextcloudStorage` shell is implemented; activation is credential flip
([`phase_10_nextcloud.md`](phase_10_nextcloud.md)). No silent fallback to local.

---

## Live feedback capture (open during meeting)

Title a Notes doc **J2 feedback** and capture:

- [ ] Who wears Tech / Drafter / Approver on a typical job?
- [ ] Which report sections do owners care about most?
- [ ] Threshold language they already use with clients
- [ ] Any Zoho / current-process pain to map to HTMX flows
- [ ] Timeline they want for real `.esx` + sample Word

---

## After the meeting — your single next action

Send Josh one message with asks 1–3 and a calendar hold for when assets arrive.
Do not start Hamina / AI / native app work until activation is done.
