services:
  web:
    build: .
    env_file: .env
    ports:
      - "8050:8000"
    volumes:
      - ./:/app
      - ./storage:/app/storage
    depends_on:
      - db

  db:
    image: postgres:16
    env_file: .env
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
